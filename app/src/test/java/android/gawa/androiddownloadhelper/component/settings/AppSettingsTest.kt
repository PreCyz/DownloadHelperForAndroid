package android.gawa.androiddownloadhelper.component.settings

import org.junit.Test

import org.junit.Assert.*
import org.junit.Before

class AppSettingsTest {

    private var serverSetup : ServerSetup? = null

    @Before
    fun setUp() {
        /*val serverSetup = ServerSetup()
        serverSetup.apiInfo = "apiInfo"
        serverSetup.downloadFolderName = "downloadFolder"
        serverSetup.password = "password"
        serverSetup.port = 5003
        serverSetup.username = "username"*/
    }

    @Test
    fun getServerSetup() {
        /*val appSettings = AppSettings(null)
        appSettings.serverSetup = serverSetup

        assertEquals(serverSetup, appSettings.serverSetup)*/
    }
}